git --version
git config --global user.name
git status
cd "C:\Users\Administrator\aa Git video"
git init
dir
git config --global user.name "rony38050"
git config --global user.name
git config --global user.email "rony38050@gmail.com"
git config --global user.email
git status
git add .
git add --all
git add --A
git reset
git rm --cached test.txt
git add README.md
git status
git commit -m "This is my First Commit"
git status
git branch
git branch dev
git checkout dev
git merge dev -m "merge with dev"
git branch -M main
git branch -a
git remote add origin https://github.com/rony38050/pythonNovember2025
git remote -v
git commit -m "This is my Second Commit"
git push origin main
git push origin HEAD
git push origin HEAD:main "for merge"
git clone https://github.com/rony38050/pythonNovember2025.git
git pull origin main
git fetch origin
